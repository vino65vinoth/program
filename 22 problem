 const prompt = require ("prompt-sync")();

 const num = prompt ("input=");
 const input = parseInt (num);

 if (input % 5) {
    console.log("Yes,divisible by 5")
 }
 else  {
    console.log("No,not divisible by 5")
 }
